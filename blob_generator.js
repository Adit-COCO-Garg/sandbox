'use strict';

class Blob_generator{
	// #blobType = "standard";
	// #speed = 3;
	constructor(){
		this.speed = random(5,10);
		this.type = int(random(1,5));
		this.radius = random(windowHeight*0.03, windowHeight*0.05);
		this.x = random(this.radius, windowWidth-this.radius);
		this.y = random(this.radius, windowHeight-this.radius);
    }
    
    gen_
}